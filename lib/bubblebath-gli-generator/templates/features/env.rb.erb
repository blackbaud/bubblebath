dir = File.join(File.dirname(__FILE__))
$: << File.expand_path("#{dir}/../../lib")
$: <<  File.expand_path(dir)

require '<%= name.downcase %>'
require 'bubblebath/cucumber/env'
include '<%= BubblebathGliGenerator::Project.camelize(name) %>'

